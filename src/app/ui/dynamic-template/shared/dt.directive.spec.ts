import { DtDirective } from './dt.directive';

describe('DtDirective', () => {
  it('should create an instance', () => {
    const directive = new DtDirective();
    expect(directive).toBeTruthy();
  });
});
